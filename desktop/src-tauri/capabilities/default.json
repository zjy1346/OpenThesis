{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the OpenThesis main window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
