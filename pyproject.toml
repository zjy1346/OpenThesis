[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "openthesis"
version = "2.2.1"
description = "Model-agnostic, evidence-grounded AI research for long-term investors"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "Apache-2.0"}
authors = [{name = "OpenThesis Contributors"}]
dependencies = ["pypdf>=5.0,<7.0", "pdfplumber>=0.11,<1.0", "pypdfium2>=4.30,<5.0"]

[project.scripts]
openthesis-sidecar = "openthesis.sidecar:main"

[tool.setuptools]
package-dir = {"" = "src"}
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
openthesis = ["resources/**/*"]
