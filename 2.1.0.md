# OpenThesis 2.1.0 变更记录：财报采集、连续性与研究性能重构

## 基线

- GitHub 最新正式版本：`v2.0.1`
- `main` 基线提交：`7c04b43c1a538b5e14a6deccaa51b9be4841ab37`
- 本变更以 2.0 的 `.ot` 研究包、证据校验、三语界面和视觉兜底架构为基础。
- 该范围涉及采集策略、调度、数据模型、服务 API 和界面，建议版本为 `2.1.0`。

## 用户要求

- [x] 将单家公司通常超过 20 分钟的研究时间显著压缩。
- [x] 对独立财报执行安全的并发下载和识别，不采用无界并发。
- [x] 根治港股/A 股年度缺失，特别是 2025 财年缺失。
- [x] 根治收入增长缺失以及跨断档误算。
- [x] 根治 ROE 等指标缺失，并显示具体数据质量原因。
- [x] 财报数据不完整时始终显示可见的重试入口。
- [x] 对确定性财报环节自动重试且不消耗额外模型 Token。
- [x] 解释并重构 OT Studio“研究周期”和财报数据范围的关系。
- [x] 明确模型实际收到哪些数据，禁止无来源数字进入确定性报告。
- [x] 年报下载和重试必须从采集架构根治，不能只修改报告 HTML。

## 2.0.1 已确认的根因

### 1. 港股报告期被错误假定为自然年

`src/openthesis/market_data.py` 的 `_report_period_end` 将完整财年固定成 `12-31`，并从标题中抓取首个四位年份作为财年。阿里巴巴等非自然年财年公司不能用该规则归期；标题同时出现多个年份时也可能选择错误年份。

`src/openthesis/filing_selection.py` 随后按 `(推断财年, 财务期间)` 去重。上游财年一旦误标，两份不同报告可能发生错误碰撞，其中一份被覆盖，从而出现 `2026、2024、2023、2022` 而没有 2025。

### 2. 获取数量固定为 5

`src/openthesis/service.py` 对 SEC、A 股和港股均固定调用 `limit=5`。采集计划没有建立“展示 N 年必须额外获取 N+1 比较年”的规则，也没有在连续性校验后定向补抓缺失年份。

### 3. 年度同比跨断档计算

`src/openthesis/financials.py::calculate_metrics` 使用排序列表中的下一条记录作为 `previous`，没有严格查找 `year - 1`。因此 2025 缺失时，2026 仍可能与 2024 计算并显示为同比；最旧展示年度没有增长，则是因为没有额外比较基期。

### 4. 财报链路仍然串行

`src/openthesis/service.py` 逐份下载财报；`src/openthesis/financial_ingestion.py::FinancialIngestionEngine.ingest` 也逐份执行结构化来源、PDF AST、视觉回退和校验。现有 `ThreadPoolExecutor` 主要用于模型 Agent，并未加速财报链路。

### 5. 隔离粒度和重试能力不足

财务校验出现问题时可能隔离整组事实；ROE 缺少 `reported_roe` 或 `net_income / equity` 的必要输入时只得到空值，页面没有字段级原因。服务目前只提供综合报告重试和增长机会重试，没有“仅重新发现、下载、解析缺失财报”的后端能力。

### 6. OT Studio 研究周期与采集范围脱节

`desktop/src/features/ot-studio/OtStudioView.tsx` 明确说明 `horizon_years` 只控制研究与情景分析跨度，不决定下载多少年财报；采集端仍固定为 5 份。模型只获得实际传入的财务事实、披露证据、市场数据和用户输入。研究周期超过证据覆盖期时只能做情景推演，不能获得不存在的历史财报。

2.0.1 已校验证据 ID，但若上游财年归类或确定性计算错误，模型仍会使用“有来源但语义错误”的数据，因此必须先修复证据管线。

## 2.1.0 方案

### A. 分离分析周期与证据历史

1. 保留 `horizon_years`，界面改称“结论/情景周期”。
2. 为 `.ot` 增加向后兼容的 `evidence_policy.annual_history_years`。
3. 另设季度/中期策略，研究开始前展示预计抓取计划。
4. 旧 `.ot` 包缺少新字段时使用稳定默认值。

### B. 建立市场无关的 `FilingPlan`

1. 保存并验证公司财年结束月日；未知时从连续官方报告推断。
2. 分开建模公告日期、标题年份、报告期结束日、财年标签和报告类型。
3. 先发现足够候选，再规范化、去重和选择，不在发现阶段直接截断为 5。
4. 展示 N 年时至少获取 N 个连续完整财年加 1 个隐藏比较财年。
5. 连续性检查发现缺年后按年份定向补查。
6. 季度/中期累计数据禁止混入完整财年表。

### C. 有界并发流水线

1. 官方来源元数据查询可并发，但每个域名独立限流。
2. PDF 下载采用有界 I/O 并发，初始建议全局 3、单域名 2。
3. 本地 PDF 解析按文档并行；CPU 密集任务采用有界进程池。
4. 视觉兜底继续遵守逐页授权、数量上限和服务限流。
5. 同一 URL/内容哈希使用 single-flight 和缓存，禁止重复下载、解析。
6. 合并、期间归类、跨表校验和证据快照冻结保持确定性顺序。
7. 支持取消、独立失败和检查点恢复。

### D. 零模型 Token 自动恢复

1. 仅自动重试来源发现、网络下载、缓存读取、PDF 解析、字段映射和确定性校验。
2. 临时网络错误使用带抖动的指数退避，永久错误不盲目重试。
3. 解析回退顺序：缓存原文 → 备用官方入口 → 本地备用解析器 → 经用户逐页授权的云端视觉解析。
4. 自动恢复不得调用主研究模型，模型调用计数和 Token 增量必须为 0。
5. 手动重试只重跑失败/缺失节点，不重新执行成功节点或模型 Agent。

### E. 精确同比和字段级质量状态

1. 同比只能匹配同公司、同口径、同币种的 `fiscal_year - 1`。
2. 断档时不计算跨年“同比”，明确显示缺少哪个基期。
3. 指标保存分子、分母、公式、期间、单位和证据 ID。
4. ROE 不可计算时显示缺失输入，不再只显示横线或伪造 0.0%。
5. 将质量结果分成字段级、报表级和期间级错误；非关键字段失败不清空整组可信事实。

### F. 财报状态与重试 API

1. 后端暴露财报计划、节点状态、缺失期间、错误类别和重试次数。
2. 增加“仅重试失败/缺失财报”的幂等、零 AI 调用 API。
3. 增加需二次确认的“强制重新下载并完整重建”。为避免网络失败造成不可逆数据丢失，不预删旧缓存；新结果逐份通过校验后才原子替换旧结果。
4. 财报不完整、字段隔离或校验失败时，报告页始终显示重试入口。
5. 报告生成后仍可修复财报，并由用户选择是否用新证据重建报告。

### G. 模型数据边界

1. AI 启动前冻结不可变 `EvidenceSnapshot`。
2. 区分官方财报、市场数据、用户输入、确定性计算和模型推断。
3. 每个确定性数字携带来源、报告期、口径、币种、单位和证据 ID。
4. 验证器同时检查证据 ID、引用期间和引用数值是否一致。
5. 证据历史短于结论周期时明确披露，禁止模型填补缺失年度。

## 验收清单

### 财年与指标

- [x] `09988.HK` 按非自然财年正确归期，连续年度不丢 2025。
- [x] 两份标题年份相同但报告期不同的文件不会错误去重。
- [x] 2025 缺失时 2026 不与 2024 计算同比。
- [x] 展示 N 个财年时自动获取 N+1 个合格年度。
- [x] 最新中期/季度数据不进入完整财年表。
- [x] ROE 可计算时正确展示，不可计算时展示具体原因。
- [x] 单字段失败不会无理由清空同年度其他已验证字段。

### 性能与恢复

- [x] 对相同公司完成冷缓存串行/并发基准并记录各阶段耗时。
- [x] 并发不重复请求，也不超过单域名上限。
- [x] 一个文档失败不取消其他成功任务。
- [x] 自动财报重试的模型调用与 Token 增量均为 0。
- [x] 手动重试只处理失败或缺失节点。
- [x] 取消研究能取消未开始任务并安全收束运行中任务。

### UI、OT 与证据

- [x] “结论/情景周期”和“财报证据历史”独立且解释清楚。
- [x] 修改证据历史会同步改变预计采集计划。
- [x] 缺失、隔离或失败时始终可见财报重试按钮。
- [x] 用户能查看缺失财年、失败阶段、尝试次数和下一步操作。
- [x] 每个展示数字可追溯，模型不能将无对应期间/数值证据的数字写入确定性章节。

### 回归与发布

- [x] 覆盖美股、A 股、港股、非自然财年、修订报告和重复公告。
- [x] 覆盖网络超时、部分下载失败、解析失败、缓存恢复和视觉回退。
- [x] Python 测试、桌面端测试、lint、typecheck、build 全部通过（仓库未定义独立 lint 命令；使用 `compileall`、`git diff --check`、`tsc -b` 和 Rust 编译测试完成等价静态验收）。
- [x] 逐条对照本文件验收后打包；用户测试通过并授权后再提交、推送和发布。

## 实施与验收证据

- 冷缓存模拟基准（同一公司 6 份独立财报）：串行下载 `1.202s`，有界并发 `0.603s`，约 `1.99x`；全局峰值 `3`、单域名峰值 `2`，结果顺序稳定，无重复请求。
- 大型 PDF 采用自适应策略：并发下载；小型独立文件可使用最多 2 个解析进程；压缩文件总量超过 8 MiB 时顺序解析，避免内存峰值抵消并发收益。
- 真实官方 PDF 已逐公司独立进程通过：宁德时代、贵州茅台、五粮液、金博股份、比亚迪、中芯国际、阿里巴巴、腾讯、美团；包含 A 股、港股、非自然财年、中期比较与三表来源页证据。
- 宁德时代 ROE 回归：预扫描现保留正式三表页面及独立 ROE 摘要页，2025 披露加权平均 ROE `24.91%` 可追溯到原页与坐标。
- 比亚迪回归：表格全局“人民币千元”单位标记不再替代年度数值列中心，收入和经营现金流不会误取附注编号。
- Python：核心分组 `176/176` 通过；财报引擎普通/并发单元测试 `40/40` 通过；大型真实 PDF 按公司独立通过。
- 桌面端：Vitest `63/63`、`tsc -b`、Vite production build 通过。
- Rust/Tauri：`cargo test` `29/29` 通过；测试构建和链接成功。
- 代码卫生：2.1.0 范围 `git diff --check` 通过。已知无关用户文件 `docs/releases/website-1.2.1.md` 的既有尾随空格未修改。
- 完整重建需界面及后端双重确认；重建不预删旧缓存，新文件成功且通过校验后才按财报节点原子替换。普通重试仍只处理失败/缺失节点。
- 财报修复后若事实 ID 集合与原研究快照不同，报告会明确标记旧 AI 结论仍基于原快照；用户可保留历史报告或从“新研究”重新生成结论。

## 当前状态

- 已基于 GitHub `v2.0.1` 及其后续 `main` 文档提交完成第一轮源码诊断。
- 用户已明确确认实施 `2.1.0`。
- 代码实现、逐项验收和 Windows 便携版打包均已完成。
- 测试制品：`installer-output/OpenThesis-2.1.0-windows-x64-portable.zip`。
- SHA-256：`8EFCDA6C5533F3429720BBA44FA85137E9F050FCD5A9C95266458B04E93B2C0D`。
- 制品隐私检查无命中，便携结构完整；实际启动验证确认主程序与 sidecar 正常、无可见命令行窗口。
- sidecar 打包清单已排除 Windows 自带的 `ucrtbase.dll`，避免开发环境误带第三方系统运行库副本导致实时防护锁定或版本偏差。
- 未获用户测试通过及发布授权前，不提交、推送或发布 GitHub Release。
